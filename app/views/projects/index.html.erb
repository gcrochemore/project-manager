<h1><%= t('.title') %><span class="badge"><%= @projects.count %></span></h1>

<div class="row">
  <div class="col-2">
    <legend class="filter-title"><%= t :search_title %></legend>
    <fieldset class="filter-panel">
      <%= search_form_for @q do |f| %>

        <%= f.search_field :name_cont, {placeholder: t('activerecord.attributes.project.name')} %>

        <%= f.search_field :organization_cont, {placeholder: t('activerecord.attributes.project.organization')} %>

        <%= f.search_field :bug_tracker_cont, {placeholder: t('activerecord.attributes.project.bug_tracker')} %>

        <br>
        <br>
        <%= f.submit class: 'btn btn-search-filter btn-sm btn-outline-primary' %>
      <% end %>
    </fieldset>
  </div>
  <div class="col">
    <div class="col-md-3 btn-new-top">
      <% if can? :create, Project %>
        <%= link_to (fa_icon('plus') + " " + t('.new_button')).html_safe, url_for(action: :new), class: "btn btn-sm btn-outline-primary" %>
      <% end %>
    </div>
    <table class="table table-hover table-sm table-bordered">
      <thead>
        <tr>
    
          <th><%= sort_link(@q, :name, default_order: :asc) %></th>
    
          <th><%= sort_link(@q, :organization, default_order: :asc) %></th>
    
          <th><%= sort_link(@q, :bug_tracker, default_order: :asc) %></th>
    
          <th class="col-xs-1"></th>
        </tr>
      </thead>

      <tbody>
        <% @projects.each do |project| %>
          <tr>
    
            <td><%= project.name %></td>
    
            <td><%= project.organization %></td>
    
            <td><%= project.bug_tracker %></td>
    
            <td>
              <%= link_to fa_icon('eye').html_safe, project %>
              <% if can? :edit, Project %>
                <%= link_to fa_icon('pencil').html_safe, edit_project_path(project) %>
              <% end %>
              <% if can? :destroy, Project %>
                <%= link_to fa_icon('times').html_safe, project, method: :delete, data: { confirm: 'Are you sure?' } %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= will_paginate @projects %>
  </div>
</div>